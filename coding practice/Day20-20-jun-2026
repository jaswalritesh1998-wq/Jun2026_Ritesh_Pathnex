Day 20 (Debug day)
Ansible- 
---
-name: Install stuff
hosts: all 
become: yes 

tasks:
-name: Install Apache
yum:
name: httpd
state: presnt 

Terraform 
resource "aws_instance""Bad"{
ami  = "ami-123"
instance_type = "c61.8xlarge"

}

Kubernetes 

   apiVersion: apps/v1
   kind: Deployment
   metadata:
   name: broken-app
   spec:
   replicas: 3
   selector:
   matchLabels:
   app: broken
   template:
   metadata:
   labels:
   app: broken
   spec:
   containers:
   -name: app
   image: nginx

   Jenkins Pipeline — Using Credentials

   pipeline{
   agent any{
   enviroment{NSTITUTE_NAME = "Pathnex"}
   Stages{
       stage(stage('checkout'){
       steps{ git url: 'https://github.com/Pathnex/sample-java-app.git' 
       }
       }
     stage('Use Secret'){
     {withCredentials([string(credentialsId: 'PATHNEX_API_KEY', variable: 'API_KEY')])
     }
     {
     sh 'echo "Using secret $API_KEY for $INSTITUTE_NAME"' }
     }
   }
   }
   }

   gitlab containers
   
stages:
build

variables: 
INSTITUTE_NAME: "Pathnex" 
API_KEY: "PLACEHOLDER_SECRET"

build: 
stage: build 
image: alpine:latest 
script: - echo "Using secret $API_KEY for $INSTITUTE_NAME"

